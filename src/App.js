import { useWallet } from "use-wallet";
import { Container } from "@chakra-ui/react";

function App() {

  const wallet = useWallet();

  return (
    <Container>
    </Container>
  );
}

export default App;
